import {createClient} from '@supabase/supabase-js';
export class ApiError extends Error{constructor(public status:number,message:string){super(message)}}
export function config(){return{supabaseUrl:process.env.NEXT_PUBLIC_SUPABASE_URL||process.env.SUPABASE_URL||'',supabaseKey:process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.SUPABASE_ANON_KEY||''}}
export async function userClient(request:Request){const c=config();if(!c.supabaseUrl||!c.supabaseKey)throw new ApiError(503,'Account sync is not configured yet. Your guest session is not saved to the cloud.');const auth=request.headers.get('authorization');if(!auth?.startsWith('Bearer '))throw new ApiError(401,'Sign in to save your progress.');const token=auth.slice(7);const client=createClient(c.supabaseUrl,c.supabaseKey,{global:{headers:{Authorization:auth}},auth:{persistSession:false,autoRefreshToken:false}});const{data,error}=await client.auth.getUser(token);if(error||!data.user)throw new ApiError(401,'Your session has expired. Sign in again.');return{client,user:data.user}}
export async function body(request:Request){if(Number(request.headers.get('content-length')||0)>25000)throw new ApiError(413,'This request is too large.');const text=await request.text();if(text.length>25000)throw new ApiError(413,'This request is too large.');try{return JSON.parse(text)}catch{throw new ApiError(400,'Send valid JSON.')}}
export function failure(e:unknown){if(e instanceof ApiError)return Response.json({error:e.message},{status:e.status});return Response.json({error:'This action could not be completed. Please try again.'},{status:500})}
export function sameOrigin(request:Request){const origin=request.headers.get('origin');if(origin&&origin!==new URL(request.url).origin)throw new ApiError(403,'This request must come from CourseCompass AI.');}
